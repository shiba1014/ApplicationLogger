# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ApplicationLogger' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ApplicationLogger
  pod 'GTMAppAuth'

  target 'ApplicationLoggerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ApplicationLoggerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
